apiVersion: v1
kind: Service
metadata:
  name: calculator-service
  labels:
    app: calculator-service
spec:
  selector:
    app: calculator-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP